.git
.gitignore
__pycache__
.venv
.pytest_cache
*.pyc
.env
.DS_Store
# tests/ is intentionally NOT ignored — the Dockerfile `test` stage runs pytest
# against it as a pre-deploy gate. pytest itself stays out of the runtime image.
logs/
data/
